import React from 'react'

import styles from 'style/Header.module.css'

const Header = () => {
  return (
    <header className={styles.Header}>
      <h1 className={styles.HeaderTitle}>StackOverflow</h1>
    </header>
  )
}

export default Header
